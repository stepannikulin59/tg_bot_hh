services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - tg_bot_hh_state:/data

volumes:
  tg_bot_hh_state:
